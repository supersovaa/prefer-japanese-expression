---
name: prefer-japanese-expression
description: 日本語の文章で、日本語で十分に表現できる概念について、日本語表現を優先する。英語の単純なカタカナ転写は目的にせず、表記自体に意味がある英語は維持する。
---

# Prefer Japanese Expression

日本語の文章では、日本語として十分に表現できる概念について、日本語表現を優先する。

このスキルの目的は、英字を減らすことでも、文章全体を自然な日本語へ書き換えることでもない。
英語表現と日本語表現のどちらを使うかという選択だけを扱う。

## Core rule

日本語の地の文で、意味を損なわず日本語として表現できる概念は、日本語で書く。

英単語を単にカタカナへ転写する必要はない。
日本語化が実質的に音写だけになる場合は、英字表記を維持してよい。

英語の名詞句に日本語の助詞を付けただけの表現より、その概念を日本語で表した文を優先する。

## Decision rule

英語表現を使おうとしたときは、次の順で判断する。

1. その表記自体を参照する必要があるか確認する。
2. 表記自体を参照する必要がなければ、その概念を日本語で十分に表現できるか確認する。
3. 十分に表現できるなら、日本語を使う。
4. 日本語化が単なるカタカナ転写になるだけなら、英字表記を維持してよい。
5. 日本語化によって識別性、正確性、対応関係が失われる場合は、原表記を維持する。

## Preserve original notation

次のような表記は、必要に応じて原表記を維持する。

- コード
- 識別子
- 関数名
- 変数名
- 型名
- クラス名
- ファイル名
- パス
- コマンド
- コマンドライン引数
- API 名
- 製品名
- サービス名
- 固有名詞
- 規格名
- プロトコル名
- 状態値
- スキーマ上の値
- 引用中の表記
- ユーザーが明示的に指定した表記

原表記を維持できることは、その周辺の説明まで英語にする理由にはならない。

## Prefer semantic translation

単語ごとの置換より、その文脈で表している意味を日本語にする。

### Example

Bad:

`semantic impact` 判断は `GPT responsibility` に残す。

Better:

意味上の影響の判断は GPT 側に委ねる。

Bad:

旧 `--base-file` / `--base-blob-sha` 型 `interface` がなくなった。

Better:

旧 `--base-file` / `--base-blob-sha` 形式の引数指定がなくなった。

Bad:

`backend` を選択して `base state` を取得する。

Better:

取得方法を選び、基準状態を取得する。

## Typical replacements

文脈に合う場合は、次のような日本語表現を優先する。

- rule → 規則
- context → 文脈
- output → 出力
- default → 既定
- behavior → 動作
- dependency → 依存関係
- implementation → 実装
- current implementation → 現行実装
- design → 設計
- canonical design → 正式な設計 / 基準となる設計
- completion condition → 完了条件
- implementation task → 実装作業
- base state → 基準状態
- semantic impact → 意味上の影響
- responsibility → 責任 / 責任範囲
- current architecture → 現行の構成
- repository state → リポジトリの状態
- source path → 実装先 / ソースパス

これは固定の対訳辞書ではない。
実際の文脈に適した日本語を選ぶ。

## Do not force katakana

英単語をカタカナにするだけでは、このスキルの目的を満たさない。

例えば、文脈上「取得方法」と書ける `backend` を「バックエンド」に置き換えることを優先しない。

一方、日本語として適切な表現がなく、一般的な日本語表現も単なる音写に近い場合は、英字表記を残してよい。

## Scope boundary

このスキルは、次のことを目的にしない。

- 文章全体の自然さの改善
- 文体の統一
- 敬語の調整
- 冗長表現の削減
- AI らしい文章表現の除去
- 技術用語そのものの排除
- 英字やカタカナの全面禁止

必要なのは、日本語で十分に表現できる概念を、不要に英語のまま残さないことだけである。
